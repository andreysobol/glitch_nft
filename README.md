# IPFS: Add content

Upload images directory to IPFS (recursively):

```shell
cd content/images
ipfs add -r .
```

Upload metadata directory to IPFS (recursively):

```shell
cd content/metadata
ipfs add -r .
```